import React, { useEffect, useRef } from "react";

const UseRefBasics = () => {
  return (
    <>
      <form className="form">
        <div>
          <input type="text" />
          <button type="button">submit</button>
        </div>
      </form>
    </>
  );
};

export default UseRefBasics;
